/*
 * Copyright(c) Beijing Kungeek Science & Technology Ltd.
 *
 */
package com.kungeek.ftsp.api.infrastructure.core.mvc.handler.requestargs;

import java.lang.annotation.Documented;
import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;
import java.util.Collection;
import java.util.List;

/**
 * Ftsp自定义返回JSON集合注解。
 * <p>对于集合类，Jackson无法直接将对应的JSON字符串转换为集合对象，需要指定集合和集合元素的类型，所以需要处理。</p>
 * <p>由于FTSP API最常用的是列表List，所以默认的集合是List,如果需要转换的对象是List,则collection不需要填写，
 * 如果是其他集合类如Set等，则需要专门指定。</p>
 * <p>如果变量的名字与请求的参数名一致，则不需要填写value，否则需要指定</p>
 * <p>示例：</p>
 * <ul>
 * <li>变量名与请求参数名一致，集合为List
 * <pre>
 * public void foo(@FtspCollectionJson(element=Bar.class) List<Bar> barList){
 * }
 * </pre>
 * </li>
 * <li>变量名与请求参数名不一致
 * <pre>
 * public void foo(@FtspCollectionJson(value="bar",element=Bar.class) List<Bar> barList){
 * }
 * </pre>
 * </li>
 * <li>变量名与请求参数名不一致，集合不为List
 * <pre>
 * public void foo(@FtspCollectionJson(value="bar",collection = Set.class,element=Bar.class) Set<Bar> barSet){
 * }
 * </pre>
 * </li>
 * <p>
 * </ul>
 * @version 3.1
 * @see FtspJsonMethodArgumentsResolver
 */
@Target(ElementType.PARAMETER)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface FtspCollectionJson {

    String value() default "";

    /**
     * Whether the parameter is required.
     * <p>Defaults to {@code true}, leading to an exception being thrown
     * if the parameter is missing in the request. Switch this to
     * {@code false} if you prefer a {@code null} value if the parameter is
     * not present in the request.
     * <p>Alternatively, which implicitly
     * sets this flag to {@code false}.
     */
    boolean required() default true;

    Class<? extends Collection> collection() default List.class;

    Class<?> element();
}
